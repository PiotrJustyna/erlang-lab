{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build hello_world",
            "type": "shell",
            "command": "rebar3 compile",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$erlang",
            "options": {
                "cwd": "${workspaceRoot}/1_hello_world"
            }
        },
        {
            "label": "build generic_server_1",
            "type": "shell",
            "command": "rebar3 compile",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": "$erlang",
            "options": {
                "cwd": "${workspaceRoot}/2_generic_server_1"
            }
        }
    ]
}